name: "build-site"
on:
  push:
    branches:
      - publish

jobs:
  build: 
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2-beta
      with:
        node-version: '15'
    - name: install deps
      run: npm ci